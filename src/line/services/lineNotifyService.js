// // // server/src/services/lineNotifyService.js
// const { pushDone } = require('../utils/lineClient');

// exports.notifyStatusChange = async (lineUserId, message) => {
//   if (!lineUserId) return;
//   await pushDone(lineUserId, message);
// };
